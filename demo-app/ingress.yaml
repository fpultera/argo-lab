apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: app-ingress
  namespace: app-demo
  annotations:
    argocd.argoproj.io/sync-wave: "0"   # 👈 se aplica primero en ArgoCD
spec:
  ingressClassName: nginx
  rules:
    - host: app-demo.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: app-stable
                port:
                  number: 80
